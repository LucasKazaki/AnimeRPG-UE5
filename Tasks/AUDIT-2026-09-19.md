# Source verification repair packet, 2026-09-19

Lucas requested an audit and GitHub fixes for both AnimeRPG repositories.
Use an isolated audit branch based on master at
`98fc268cc4515e26a3a4a45158ca255cf0e7768d`.

Allowed paths: `Scripts/validate_project.py`, `Tests/test_validate_project.py`,
`.github/workflows/source-checks.yml`, `AGENTS.md`, `README.md`, this task, and
`Docs/AUDIT-2026-09-19.md`. Do not modify Source/, Config/, or the engine association.

Fix the absence of automated source/preflight checks, ambiguous native validation
instructions, and missing repository-specific agent boundaries. No new gameplay.

Run `python Scripts/validate_project.py` and
`python -m unittest discover -s Tests -p "test_*.py" -v`.
Native Game/Editor builds and interactive checks require the correct installed
engine and an owned Windows worktree. They remain separate from source-only CI.
Do not claim them from Python results. Independent review and Lucas's merge
approval remain required. No release or local runtime operation is authorized.
